mu1=5;
sigma1=2.2;
alpha1=0.22;
mu2=10;
sigma2=1.2;
alpha2=0.38;
mu3=15;
sigma3=3.8;
alpha3=0.4;
f=@(x1,x2,x3)(alpha1.*normpdf(x1,mu1,sigma1)+alpha2.*normpdf(x1,mu2,sigma2)+alpha3.*normpdf(x1,mu3,sigma3)).*(alpha1.*normpdf(x2,mu1,sigma1)+alpha2.*normpdf(x2,mu2,sigma2)+alpha3.*normpdf(x2,mu3,sigma3)).*(alpha1.*normpdf(x3,mu1,sigma1)+alpha2.*normpdf(x3,mu2,sigma2)+alpha3.*normpdf(x3,mu3,sigma3));
c=800;
p=9+c;
x1min=-sqrt(p);
x1max=sqrt(p);
x2min=@(x1)-sqrt(p-x1.^2);
x2max=@(x1)sqrt(p-x1.^2);
x3min=@(x2,x1)-sqrt(p-x2.^2-x1.^2);
x3max=@(x2,x1)sqrt(p-x2.^2-x1.^2);
Q=integral3(f,x1min,x1max,x2min,x2max,x3min,x3max)